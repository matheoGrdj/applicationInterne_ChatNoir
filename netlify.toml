# filepath: netlify.toml
[build]
  command   = "nuxt build"
  publish   = "dist"

[build.environment]
  NODE_ENV     = "production"
  NITRO_PRESET = "netlify"

[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200